apiVersion: v1
kind: Service
metadata:
  name: {{ include "nebari-chat.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "nebari-chat.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "nebari-chat.selectorLabels" . | nindent 4 }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
